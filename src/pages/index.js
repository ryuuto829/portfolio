import React from 'react';
import { Hero, Projects, About, Contact } from '@components';

const MainPage = () => (
  <>
    <main>
      <Hero />
      <Projects />
      <About />
      <Contact />
    </main>
  </>
);

export default MainPage;
